# 住院患者死亡风险预测系统使用指导手册

## 一、系统简介
本系统是一款基于机器学习技术开发的住院患者死亡风险预测工具。它能够帮助医护人员在患者入院初期快速评估患者的死亡风险，从而为临床决策提供数据支持。系统提供了两种数据输入方式，以满足不同的使用场景和需求。

请下载我们的项目通过以下方式：

点击下载链接：https://codeload.github.com/Delysid749/Patient-Survival-Prediction/zip/refs/heads/main
使用git克隆本项目（推荐）：git clone https://github.com/Delysid749/Patient-Survival-Prediction.git

## 二、环境准备

### 硬件要求
- **内存**：推荐4GB RAM以上。
- **存储**：足够的磁盘空间以存储数据和模型文件。

### 软件要求
- **操作系统**：Windows 10/11
- **浏览器**：Google Chrome、Mozilla Firefox 或 Microsoft Edge 等现代浏览器
- **Python环境**：请确保已安装以下依赖库，推荐使用Anaconda进行环境管理

| 库名称        | 版本要求 | 用途                        |
| ------------- | -------- | --------------------------- |
| numpy         | >=1.21.0 | 基础数据处理和科学计算      |
| pandas        | >=1.3.0  | 数据处理和分析              |
| scikit-learn  | >=0.24.2 | 机器学习算法和工具          |
| xgboost       | >=1.5.0  | 提升树算法实现              |
| lightgbm      | >=3.3.0  | 轻量级梯度提升框架          |
| tensorflow    | >=2.8.0  | 深度学习框架（用于DNN模型） |
| streamlit     | >=1.10.0 | Web应用框架                 |
| matplotlib    | >=3.4.3  | 数据可视化                  |
| seaborn       | >=0.11.2 | 数据可视化增强              |
| cloudpickle   | >=2.0.0  | 模型序列化和反序列化        |
| pathlib       | >=1.0.1  | 路径操作                    |
| python-dotenv | >=0.19.0 | 环境变量管理                |
| jupyter       | >=1.0.0  | Notebook环境（可选）        |
| notebook      | >=6.4.0  | Notebook服务器（可选）      |

### 环境搭建步骤
1. **安装Python**：从[Python官网](https://www.python.org/)下载并安装Python，推荐使用Python 3.8或更高版本。
2. **安装Anaconda**：从[Anaconda官网](https://www.anaconda.com/)下载并安装Anaconda，这将帮助您更方便地管理Python环境和依赖包。
3. **创建虚拟环境**：在Anaconda Prompt中运行以下命令创建一个新的虚拟环境：
   ```
   conda create -n patient_risk_env python=3.9
   ```
   然后激活虚拟环境：
   ```
   conda activate patient_risk_env
   ```
4. **安装依赖包**：在虚拟环境中，使用以下命令安装所需的依赖包：
   ```
   pip install numpy>=1.21.0 pandas>=1.3.0 scikit-learn>=0.24.2 xgboost>=1.5.0 lightgbm>=3.3.0 tensorflow>=2.8.0 streamlit>=1.10.0 matplotlib>=3.4.3 seaborn>=0.11.2 cloudpickle>=2.0.0 pathlib>=1.0.1 python-dotenv>=0.19.0 jupyter>=1.0.0 notebook>=6.4.0
   ```


### 启动系统
- **命令行启动**：在命令提示符或终端中运行以下命令：
  ```
  streamlit run .\patient_risk_interface.py
  ```
- **快捷方式启动**：我们还提供了一个批处理文件`start_web.bat`，您可以直接双击该文件来快速启动系统。

## 三、操作步骤

### （一）访问系统
在浏览器地址栏输入系统网址，进入系统页面。

### （二）选择数据输入方式
系统提供了两种数据输入方式，您可根据实际情况进行选择：
- **手动输入单个患者**：适用于单个患者的风险预测。
- **上传CSV文件批量预测**：适用于批量患者的死亡风险预测。

### （三）手动输入单个患者信息
1. **选择输入方式**：点击左侧“手动输入单个患者”选项。
2. **输入患者特征信息**：根据系统提示，逐项输入患者的相关特征信息，包括数值型特征和分类型特征。
3. **发起预测**：点击“预测患者死亡风险”按钮，系统将自动进行预测。
4. **字段介绍**：详细的介绍请查看项目文件col_description.md

### （四）上传CSV文件进行批量预测
1. **选择输入方式**：点击左侧“上传CSV文件批量预测”选项。
2. **准备CSV文件**：确保CSV文件包含模型所需的字段，且数据格式正确。
3. **上传文件**：点击“选择CSV文件”按钮，从本地选择准备好的CSV文件并上传。
4. **发起预测**：系统将自动进行预测，并在预测完成后显示结果。

## 四、结果解读
预测结果将显示在页面上，包括预测的死亡风险概率和生存结果。死亡风险概率是一个介于0和1之间的数值，表示患者死亡的可能性大小；生存结果则直接显示为“存活”或“死亡”。

## 五、注意事项
1. **数据准确性**：输入的数据应尽可能准确和完整，以确保预测结果的可靠性。
2. **结果参考**：预测结果仅供参考，最终的医疗决策应由专业医护人员根据实际情况综合判断。

   

